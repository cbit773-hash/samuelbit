# Frontend (público — seguro en el bundle)
VITE_SUPABASE_URL=https://rierlbcvpvfxkffxnyup.supabase.co
VITE_SUPABASE_ANON_KEY=eyJh...
VITE_APP_URL=http://localhost:5173
# Shell Fortrade unificada (false = layout legacy)
VITE_FORTADE_SHELL=true

# Auth: redirect de recuperación de contraseña → /auth/restablecer
# Supabase Dashboard → Auth → URL Configuration (Site URL + Redirect URLs)
# Desactivar "Confirm email" para login inmediato tras /registro
# Ver docs/GUIA_REGISTRO_AUTH.md y docs/SUPABASE_AUTH_CHECKLIST.md
# No existe modo demo RBAC — solo login/registro real

# Seed local de usuarios @investpro.com (npm run seed:dev-users) — NUNCA en VITE_* ni en el frontend
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Go-live — migraciones (Dashboard → Settings → Database → password)
# SUPABASE_DB_PASSWORD=...
# SUPABASE_ACCESS_TOKEN=sbp_...   # opcional: cuenta dueña del proyecto (Edge Functions CLI)

# Cloudflare Workers (npm run deploy) — prod: https://cbit773.cbit773.workers.dev
# CF_PAGES_PROJECT=investpro

# NO poner NOWPAYMENTS_API_KEY aquí — solo en Supabase Edge Function secrets
# Ver supabase/PAYMENTS_DEPLOY.md

# Email transaccional (solo en Supabase Edge Function secrets, NO en VITE_*)
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=InvestPRO <notificaciones@investpro.com>
# Ver docs/GUIA_NOTIFICACIONES.md
